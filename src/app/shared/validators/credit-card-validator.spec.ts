import { CreditCardValidator } from './credit-card-validator';

describe('CreditCardValidator', () => {
  it('should create an instance', () => {
    expect(new CreditCardValidator()).toBeTruthy();
  });
});
