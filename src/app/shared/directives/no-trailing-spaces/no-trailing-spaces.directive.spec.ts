import { NoTrailingSpacesDirective } from './no-consecutive-spaces.directive';

describe('NoTrailingSpacesDirective', () => {
  it('should create an instance', () => {
    const directive = new NoTrailingSpacesDirective();
    expect(directive).toBeTruthy();
  });
});
