import { AutoCapitalizeDirective } from './auto-capitalize.directive';

describe('AutoCapitalizeDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoCapitalizeDirective();
    expect(directive).toBeTruthy();
  });
});
