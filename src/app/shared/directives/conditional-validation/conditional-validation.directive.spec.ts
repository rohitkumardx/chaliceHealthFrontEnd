import { ConditionalValidationDirective } from './conditional-validation.directive';

describe('ConditionalValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new ConditionalValidationDirective(null);
    expect(directive).toBeTruthy();
  });
});
