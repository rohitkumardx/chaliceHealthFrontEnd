import { SsnFormatDirective } from './ssn-format.directive';

describe('SsnFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new SsnFormatDirective();
    expect(directive).toBeTruthy();
  });
});
